project module package class
main方法是执行入口，
//*变量： 数据类型 变量名称 = 数量； int age = 18；
给变量开辟一块内存，并且赋值，
数据在计算机最小储存单元，字节b 一个字节8个b，1kb=1024b，1mb=1024kb，1g=1024mb，1t=1024gb
char ch ='A';八进制以0开头，16进制以ox开头 2进制以ob开头
byte 1个字节，kb mb gb  tb 1024
整型byte short int（默认） long 浮点数（小数) float double(默认） 字符型char 布尔型boolean true false
keywords：关键字  标识符：字母数字下划线美元
*/
变量名：首字母小写， 类名首字母大写 驼峰模式。
 ＋符号在Java中的特殊用法，做连接符，结果是字符串，能算就算，不能算就连接在一起，两个整数除法，结果一定是整数，最高类型是整数 %这个是取余数的
自增：++ 放在某个变量前面或者后面，对变量自身值加一
自减：-- 放在某个变量前面或者后面，对变量自身值减一
int a =10;
int rs =++a;
在变量前面，先对变量自增或者自减，再使用变量。在变量后面，在变量后面，变量先赋值，在进行自增或者自减
赋值运算符：="从右往左看 
+- a+=b 加后赋值 a=（a的类型）（a＋b）
关系运算符：>< >=等等 ==相等 =赋值 ！=不等于 布尔类型返回值是true false
三元运算符：条件表达式？值1：值2；
与 两者都要成立 或 二选一成立 两者都是假才是假 非 取反成立 异或 两者是相同条件就是假 两者一真一假就是真
短路与＆＆ 2》10 ＆＆3》2 判断结果和＆一样，左边为false，右边不执行 短路或11 2》1 判断结果与1一样，左边为true 右边不执行
switch分支结构是比较值是否相等，来界定执行那条分支
switch（表达式）{
case1：
执行代码；
break；
case2；
执行代码....;
break;
default:
执行代码；
}
switch表达式类型只能是byte、short、int、char、string、不支持double、float、long 
case的值只能用字面量不能用变量，正常写switch要加break，否则就会出现穿透现象
当存在多个case分支代码一样的时候，可以把代码写到一个case，其他case通过穿透case块，可以简化代码
for循环（初始化语句；循环条件；迭代语句；）{
循环体语句；
}目标：减少代码的重复填写，灵活控制程序执行。 
while循环写法：初始化语句；while（循环条件）{
循环体；
迭代语句
}
package com.itheima.loop;

        public class WhileTest5 {
            public static void main(String[] args) {
                System.out.println("需要的年数："+cale());

            }
            public  static int cale(){
                double money = 100000;
        double rate = 0.017;
        int year =0;
        while(money < 200000 ){
            year ++;
            money =money*(1+rate);
        }
        return  year;
    }
}
do——while循环
初始化语句；
do{
循环体语句；
迭代语句；
} while（循环条件）；
死循环就是一直循环下去，不停
循环嵌套 外部循环每循环一次，内部循环就要跑一把
System.out.print("x")	打印内容后不换行	连续输出同一行（如矩阵行）
System.out.println("x")	打印内容后自动换行	输出完整一行后换行
break：跳出并结束当前循环的执行，continue是用于跳出当前循环的当此执行，直接进入循环下一个
math回返回（0，1）的小数


生成随机数验证码：定义一个for循环，循环5次；随机生成012的数据，依次代表当前生成的字符是数字、大写字母、小写字母。；把012交给switch生成对应类型的随机字符；在循环外定义一个string类型变量用来连接生成随机字符；循环结束以后返回string类型即生成的验证码
package com.itheima.demo;

public class Test3 {
    public static void main(String[] args) {
        //目标：开发验证码
        System.out.println(getCode(4));



    }
    public static String getCode(int n) {
        String code = "";
        for (int i = 0; i < n; i++) {
            int number = (int)(Math.random()*3);
            switch (number){
                case 0:
                int number1 = (int)(Math.random()*10);
                code += number1;
                 break;
                case 1:
                int number2 = (int)(Math.random()*26);
                char ch= (char)(65+number2);
                code += ch;
                 break;
                case 2:
                 int number3 = (int)(Math.random()*26);
                 char ch2= (char)(97+number3);
                 code += ch2;
                 break;


            }

        }
        return code;

    }


}
数据类型[] 数组名={元素。。。。。}；
System.out.println(arr[]);


---------------------------------------------------------------------------------------------
对象：可以理解成一张表，class就是类，表的模板，每次new一下就是创建一个新表，本质就是一块内存区域，栈内存（方法的执行放在这里，先进后出，在main方法）堆内存（对象放在堆里面，在堆开辟一个空间）方法区（放类文件信息）变量存在栈里面，变量指向对象，对象在堆里面，对象找到方法和类，将方法区的方法调到栈使用，使用完清零。
对象是一个特殊的数据结构，对象是new出来的，有了类就可以创建对象

构造器
创建对象的时候，对象自动构造构造器
只有修饰符和方法名（方法名必须和类名一样）
创建对象时，同时完成对对象成员变量的初始化赋值
---------------------------------
this关键字就是一个变量，可以用在方法中，来拿当前对象
用来解决对象的成员变量与局部变量的名称方法一致，导致访问冲突问题
----------------------------------------------------------------
面向对象的三大特征:封装；继承；多态
类就是一种封装：合理隐藏，合理暴露
public私有进行修饰 get set暴露出去
---------------------------------------------------------------
类中的成员变量全部私有，提供public修饰getter/setter方法（实体类）
类中提供无参构造器有参数构造器可选
分层思想：一个执行，一个计算，一个保护数据；
仅仅只用保护存取Java类，用他来创建对象，保护某个事务数据；
------------------------------------------------------------------------
static 可以用来修饰成员变量和成员方法
按照有无static修饰，分为两种；
静态变量（类变量）实例变量（对象的变量）
有static属于类，会被类的全部对象共享
无static，属于每个对象自己的

static成员方法分类
静态方法：有static修饰成员方法，属于类
类名.静态方法
做功能＋static，属于对象不加
main方法是程序的入口

静态方法的常见应用场景
做工具类，工具类方法都是一些静态方法，每个类都是完成一个功能
实例方法需要创建对象来调用，浪费内存

静态方法中可以直接访问静态成员，不可以直接访问实例成员
实例方法既可以访问静态成员，也可以直接访问实例成员
实例方法中可以出现this关键字，静态方法中不可以出现this关键字
------------------------------------------------------------------------
继承：提高代码的复用性，extends 用这个关键字可以让一个类与另外一个类建立父子关系
public class B extends A{
}//a是父类，b是子类
子类能继承父类的非私有成员（成员变量、成员方法）
子类的对象是由子类、父类共同完成的
权限修饰符（用来限制类中成员能够被访问的范围）：private 只能本类
缺醒：本类、同一个包中的类
protected：本类，同一个包的类，子孙类中
public：任何位置
单继承：一个类只能已有一个父类
支持多层继承继承object
就近原则 super.父类
方法重写：觉得父类不好，重写一个父类
子类重写父类的方法要大于等于父类的权限
重写的方法返回值类型小于等于父类范围
私有方法、静态方法不能被重写，重写就会报错
子类构造器：先调用子类构造器再执行自己
super()调用父类有参构造器的常见应用场景是什么？
为对象中包含父类这部分的成员变量进行赋值
this（...)调用本类中的其他构造器
this和super不能同时出现，必须放第一行
----------------------------------------------------------------------------
多态：在继承和实现情况下一种现象 表现形式：对象多态、行为多态
要有继承关系，父类引用只用子类对象，存在方法重写，
多态是对象、行为多态，Java中属性（成员变量）不谈多态
编译看左，运行看右。成员变量看左边，成员方法看右边
在多态形态下，右边对象是解耦合的，更便于拓展与维护
多态 = 编译时依赖抽象，运行时绑定实现。
多态下类型转换问题
自动类型转换 父类 变量名=new 子类（）；
pepole p =new Teacher();
子类 变量名=（子类）父类变量
Teacher t =（Teacher）p;
存在继承/实现关系就可以在编译阶段进行强制类型转换，编译阶段就不会报错
运行时，如果发现对象的真实类型与强转后的类型不同，就会报类型异常（classCastException）
强制转换前前判断对象的真实类型，在进行强制类型转换可以调用子类独有功能
判断：instanceof判断当前对象类型：对象 instanceof 类型
--------------------------------------------------------------------------------------------
final关键字：最终类，不能继承
修饰方法不能被重写，只能被赋值一次
final修饰基本类型变量，变量存储的数据不能被改变
final修饰引用类型的变量，变量存储的地址不能被改变，但地址所指向对象可卡因被改变
使用了static final修饰的就是常量 ，用于记录系统的配置信息
单例类是设计模式，设计模式是一个问题通常有最优解，
单例设计模式：确保某类只创建一个对象
public class Singleton {
    // 1. 私有静态实例
    private static Singleton instance;

    // 2. 私有构造器，禁止外部 new
    private Singleton() {}

    // 3. 公共静态方法，返回唯一实例
    public static Singleton getInstance() {
        if (instance == null) {
            instance = new Singleton();
        }
        return instance;
    }
}单例类三要素
饿汉式单例：拿对象时，对象早就已经创建好了
懒汉式单例：拿对象，才开始创建对象
-------------------
修饰符 enum 枚举类名（
。。。。。。。。。；
其他成员。。。。。。。。。
）
枚举类的构造器都是私有的，枚举类对外不能创建对象
枚举类可以做信息分类和标志

abstract抽象类，修饰类 成员方法
父类知道每个子类都要做的行为，每个子类要做的情况不一样，父类就定义成抽象方法，交给子类重写
提供一个方法作为完成功能的模板，模板方法封装每个实现步骤，但允许子类提供特定步骤的实现
---------------------------
接口：interface
接口不能创建对象，抽象类也不能创建对象
接口用来被类实现，实现接口类被称为实现类，一个类同时实现多个接口
弥补了类单继承补不足，有一个类可以实现多个接口，使类的功能更加强大

